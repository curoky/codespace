[storage]
driver = "vfs"
transient_store = false
graphroot = "$PODMAN_DATA_DIR/graphroot"
runroot = "$PODMAN_RUNTIME_DIR/storage"

[storage.options.pull_options]
enable_partial_images = "true"
use_hard_links = "false"
